'use strict';

let submit = document.querySelector("#formularz");
console.log(submit);

const call = (e) => {
    e.preventDefault();

    
    let dane = document.querySelector( {
        let imie = document.querySelector("fname");
        let nazw = document.querySelector("lname");
    });
    console.log(dane.children);
    console.log(e);
}

submit.addEventListener("submit", call);
